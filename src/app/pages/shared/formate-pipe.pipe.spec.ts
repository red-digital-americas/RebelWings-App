import { FormatePipePipe } from './formate-pipe.pipe';

describe('FormatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
